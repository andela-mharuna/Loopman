import React from 'react';

const NotFoundComponent = () => (
  <div>
    <h3 style={{ textAlign: 'center' }}> 404...Looks like the page you are looking for does not exist...</h3>
  </div>
);

export default NotFoundComponent;
